import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// document.addEventListener('DOMContentLoaded', () => {
//     window.Echo.channel('device.1')
//         .listen('.data.updated', (e) => {
//             console.log(e);
//         });
// }
// )
